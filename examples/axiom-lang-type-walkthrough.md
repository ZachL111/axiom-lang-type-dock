# Axiom Lang Type Dock Walkthrough

The fixture is intentionally compact, so the review starts with the cases that pull farthest apart.

| Case | Focus | Score | Lane |
| --- | --- | ---: | --- |
| baseline | IR pressure | 231 | ship |
| stress | lowering drift | 144 | ship |
| edge | stack depth | 142 | ship |
| recovery | diagnostic reach | 198 | ship |
| stale | IR pressure | 183 | ship |

Start with `baseline` and `edge`. They create the widest contrast in this repository's fixture set, which makes them better review anchors than the middle cases.

The useful comparison is `IR pressure` against `stack depth`, not the raw score alone.
